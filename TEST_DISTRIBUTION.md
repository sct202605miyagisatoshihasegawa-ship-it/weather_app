# 知人限定テスト配布（Windows 11）

この版は個人開発中のテスト版です。配布先は知人に限定し、Windows 11 でのみ動作確認します。業務利用や重要な判断には使用しないでください。

## アプリの特徴

- OpenWeatherMap から都市ごとの気温・湿度・気圧を定期取得します。
- 取得履歴を SQLite に保存し、期間を指定してグラフ表示します。
- 仙台の気圧が約 1 時間前から 3.0 hPa 以上変動した場合に通知します。
- API キーと取得間隔はアプリ内の「設定」から入力します。

## 初回設定と利用

1. [OpenWeatherMap](https://openweathermap.org/) でアカウントを作り、Current Weather API のキーを取得します。
2. 配布された MSI を実行してインストールします。
3. アプリを起動して「設定」を開き、API キーと取得間隔を保存します。

API キーは `%LOCALAPPDATA%\WeatherApp\settings.properties` にのみ保存され、ソースコードや Git には保存されません。キーを第三者に共有しないでください。

## 未署名アプリの警告

このテスト版にはコード署名がありません。そのため Windows 11 の SmartScreen が「発行元不明」の警告を表示することがあります。配布者から受け取ったファイルであることを確認できる場合だけ、テスト目的で実行してください。自己署名証明書は受信側で信頼設定をしない限り警告解消にならないため、この限定テストでは使用しません。

## アンインストールとデータ

通常のアンインストールではアプリ本体は削除されますが、`%LOCALAPPDATA%\WeatherApp` の設定、SQLite データベース、バックアップは残ります。完全に削除するには、アンインストール後にこのフォルダを手動で削除してください。削除前に必要なバックアップを別の場所へ保管してください。

## テスト時に知らせてほしいこと

- インストール、起動、アンインストールの成否
- SmartScreen を含む警告やエラーメッセージ
- 取得ログ、グラフ、気圧通知の期待との差異
- 再現手順、日時、可能なら画面のスクリーンショット

## ライセンス

アプリは JFreeChart 1.5.4 と SQLite JDBC 3.46.1.0 を使用します。配布物に含まれる Java ランタイムおよび依存ライブラリのライセンスは、インストール先の `runtime\legal` と各ライブラリの配布情報を確認してください。
